# 测试计划
1. 在桌面浏览器新标签打开指定 work-agent 项目，确认登录状态、聊天入口和可用工具。
2. 以新对话和聊天输入方式执行此前定义的 16 条测试用例；只使用现有工单/资产数据，不创建或修改生产工单。
3. 对照实际回复和页面行为记录通过、失败、受限及证据，再输出测试报告。

## 2026-09-16 API 채팅 테스트 페이지

현재 /agent-work-api/v1/sessions/{sessionId}/messages와 /stream API를 직접 호출하는 단일 HTML 채팅창을 추가한다. 새 백엔드 기능은 만들지 않고 기존 세션 API만 사용한다.

