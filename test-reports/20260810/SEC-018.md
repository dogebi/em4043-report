# SEC-018 Webhook Secret Header伪造

| 项目 | 内容 |
| --- | --- |
| 当前状态 | FAIL |
| 执行方式 | 代码审查 |
| 需求依据 | 表 32，缺失或错误 `X-Jira-Webhook-Secret` 返回 401 且不处理业务 |
| 实际结果 | Laravel `WebhookNttJiraController::index()` 不校验 `X-Jira-Webhook-Secret`，会写入 webhook 日志并返回 success。既有 TICKET-002 也显示当前链路使用 `X-Hub-Signature`/Basic Auth 而非要求的 header。 |
| 主要证据 | `WebhookNttJiraController::index()`、既有 TICKET-002 报告 |

