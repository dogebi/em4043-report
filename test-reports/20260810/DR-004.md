# DR-004 LLM 推理服务故障恢复测试报告

## 基本信息

| 项目 | 内容 |
|---|---|
| 用例编号 | DR-004 |
| 测试标题 | LLM 推理服务故障恢复 |
| 执行日期 | 2026-08-10 |
| 当前状态 | PARTIAL |
| 结论 | 会话任务进入 Redis Streams 的代码存在，但未停止 LLM 服务，未看到固定提示“AI推理服务暂时不可用”和恢复后 2 分钟内处理积压的证据。 |

## 测试步骤与结果

| 步骤 | 结果 | 证据 |
|---|---|---|
| 停止 LLM 推理服务 | NOT RUN | 未执行外部 LLM 服务停服。 |
| 用户发送诊断请求进入 Redis Streams | PASS | `SessionService` 多处调用 `xAdd()` 写入 `stream:agent_tasks:high`。 |
| 提示 AI 推理服务暂时不可用 | FAIL | 未检索到该固定提示或等价统一降级提示。 |
| 恢复后 2 分钟内处理积压 | NOT RUN | 未执行停服和恢复计时。 |
| 自动重连 | PARTIAL | Redis 读取和任务轮询存在，但未验证 LLM 连接恢复。 |

## 关键代码位置

- `D:\dev\cscAI\esm\app\AppAgent\Services\AiWork\SessionService.php`

## 当前结论

本用例标记为 `PARTIAL`。队列化路径存在，但 LLM 故障提示和恢复 SLA 未验证。
