# SEC-005 SSO登录验证完整性

| 项目 | 内容 |
| --- | --- |
| 当前状态 | PARTIAL |
| 执行方式 | 代码审查 + 既有手动 token 证据 |
| 需求依据 | 表 29，Jira SSO 登录后 Token 中 sub/email/role 正确，jira_account_id 关联正确，权限与 Jira 一致 |
| 实际结果 | 手动 token payload 可看到 `sub` 和 `app_id`，Jira cookie 自身有效；但 plugin SSO app 配置缺失导致本地 cookie-to-JWT 交换失败，payload 中未验证 email/role/jira_account_id。 |
| 主要证据 | 既有 AUTH-002 手动 token 证据、`JiraSessionTokenService` 既有检查 |

