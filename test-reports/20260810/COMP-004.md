# COMP-004 插件多标签页并行

| 项目 | 内容 |
| --- | --- |
| 当前状态 | PARTIAL |
| 执行方式 | 代码审查 |
| 需求依据 | 表 33，3 个不同 Jira 工单标签页独立使用插件，会话互不干扰，同一工单仅一个活跃会话 |
| 实际结果 | 插件注入脚本时使用 `tabId`，具备按标签页注入的基础；但配置和 JWT 存在 `chrome.storage.local` 全局存储，未发现按 Jira issue/tab 隔离会话状态的实现。未打开 3 个真实 Jira 标签页测试。 |
| 主要证据 | `background.js`、`popup.js`、`manifest.json`、插件 rg 结果 |
| 缺口 | 多标签 live 操作、按工单会话隔离、同一工单单活跃会话验证缺失 |

