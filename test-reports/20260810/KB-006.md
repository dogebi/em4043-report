# KB-006 Agent自动RAG检索

| 项目 | 内容 |
| --- | --- |
| 当前状态 | PARTIAL |
| 执行方式 | 代码审查 |
| 需求依据 | 表 26，Main Agent 阶段内自动 RAG 检索，多路召回、权限过滤、时效重排序、Top-5 注入上下文 |
| 实际结果 | worker 自动注入 `knowledge_search`，按项目解析知识库 dataset，并由 retriever 使用 reranker 后限制 Top-5；结果可写入 `knowledge_refs` 和 SSE。未发现语义+关键词多路召回、双层级权限过滤、相似度×时效性重排序的完整实现证据；未跑真实 Agent 阶段执行。 |
| 主要证据 | `KnowledgeSearchTool`、`Retriever.search(limit=PROMPT_TOP_N)`、`kb_select.resolve_project_dataset_ids()` |
| 缺口 | 多路召回、权限双过滤、时效重排、live Agent 注入验证缺失 |

